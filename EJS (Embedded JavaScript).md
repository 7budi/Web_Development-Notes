- It is a simple template engine that lets you write dynamic HTML pages using plain JavaScript. You use it with Node.js and Express to send data from a server straight into an HTML page before the browser opens it.
## How does EJS work?
- It is like having a javascript module that can run javascript code inside a html file.
- 